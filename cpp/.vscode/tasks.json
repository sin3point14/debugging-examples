{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake -DCMAKE_BUILD_TYPE=Debug . && make",
            "windows": {
                "command": "cmake -DCMAKE_BUILD_TYPE=Debug -G 'MinGW Makefiles' . && mingw32-make"
            }
        }
    ]
}